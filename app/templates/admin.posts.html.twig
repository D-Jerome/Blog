
{% extends './layouts/connection.html.twig' %}

{% block content %}    
<h1>Administration des articles</h1>         
            <a href="/blog-project/admin/post/new" class="btn btn-primary">Ajouter un article</a>
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Titre</th>
                        <th scope="col">Extrait</th>
                        <th scope="col">Publié le </th>
                        <th scope="col">Par</th>
                        <th scope="col"> </th>
                    </tr>
                </thead>
                <tbody>
                {% for post in posts %}
                {% if comment.publishState != false %}
                    <tr>
                {% else %}
                    <tr class="table-danger" >
                {% endif %}
                        <th scope="row">{{ post.id }}</th>
                        <td>{{ post.name }}</td>
                        <td>{{ post.excerptContent | raw }}</td>
                        <td>{{ post.createdAt }}</td>
                        <td>{{ post.username }}</td>
                        <td>
                            <a href="/blog-project/admin/post/modify/{{ post.slug }}/{{ post.id }}" class="btn btn-warning">Modifier</a> 
                            <a href="/blog-project/admin/post/delete/{{ post.slug }}/{{ post.id }}" class="btn btn-danger">Supprimer</a> 
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
                </table>
{% endblock %}